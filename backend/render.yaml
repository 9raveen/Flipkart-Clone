services:
  - type: web
    name: flipkart-clone-api
    env: node
    buildCommand: npm install
    startCommand: node src/seeders/seed.js && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
